// Pasta/arquivo central de utilitários puros do projeto (sem Firebase e sem UI).
// Coloque aqui funções reaproveitáveis como:
// - formatação de data/hora (ex.: formatarDataBR)
// - cálculo de IQA com base nos parâmetros coletados (ex.: calcularIqa)
// - conversão de unidades (ex.: celsiusParaFahrenheit, mgLParaUgL)
// - validações genéricas de entrada (ex.: validarEmail, validarCoordenadas)
// 
// Quando começar a implementar, exporte as funções diretamente deste arquivo
// para manter um ponto único de importação nas telas e serviços.